# Pick and match codes from a named character vector

Ensures that all necessary values are provided and prompts for a
response if they are not.

## Usage

``` r
pick_codes(dict, title, multiple = FALSE)
```

## Value

A character vector of the selected codes.
